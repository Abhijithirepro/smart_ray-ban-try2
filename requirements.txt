# The app itself runs 100% in the browser (static/ + OpenCV.js); these are only
# needed to (re)train the in-browser model and regenerate parity data via tools/.
numpy>=2.2
Pillow>=10.0
opencv-python-headless>=4.10   # >=4.10 for numpy 2.x compatibility
